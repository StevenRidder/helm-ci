# macOS
.DS_Store

# Xcode / Swift (for when app code lands)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
.build/
*.swiftpm/

# Build artifacts
build/
dist/
*.o
*.a

# Charts & weather data — large, license-bound, never commit
*.mbtiles
*.kap
*.000
*.grb
*.grb2
*.grib2
/charts/
/cache/
web/data/sat/
web/data/wxtiles/
pipeline/region.env

# Generated design archives can embed local paths; commit expanded assets instead.
docs/design/app-icons/*.zip
docs/Helm Icons/Official Icons/*.zip

# Private strategy/research/operator artifacts. Keep the public repo focused on
# runnable alpha code, safety, licensing, architecture, and public examples.
CLAUDE.md
*.docx
docs/AUDIT-*.md
docs/BUSINESS-MODEL.md
docs/BUILD-PLAN-COMMUNITY-LLM.md
docs/COMPETITIVE.md
docs/research/
docs/integrations/nfl-outreach-draft.md

# Secrets / API keys (Windy, etc.)
.env
.env.*
# the committed template carries no secrets — keep it tracked
!.env.example
*.key
secrets/

# Editor
.vscode/
.idea/

# Local agent/live-machine coordination. Keep these out of the public repo.
.claude/
AGENTS.md
