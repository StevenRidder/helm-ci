# Standard Judge Batch 003

Input status: `repaired_pending_judge_rerun` after standard repair batch 1.

Pass count: **28**
Fail count: **1**
Final-approved: **0**

## Fail List
- `BCNCON81`: BCNCON81 needs an exact local reference crop/render first; then redraw against that crop rather than approving a generic black post.

## Top Repair Themes
- BCNCON81 remains blocked by missing exact inspectable reference crop/render.
- Several pass rows still need final scale/stroke/body-slimming QA, but their semantic family and color order are now correct.
- Special-purpose rows may need a later topmark-variant policy decision; current S-101/OpenCPN row evidence supports the plain yellow simplified beacon body.

## Verdicts
- `BCNCON81` — **FAIL** (0.99): FAIL. The repaired Helm candidate is a plain black vertical post/body, but the row has no inspectable exact S-101 or AquaMap reference and the declared OpenCPN reference PNG is absent locally. For this safety-critical constructed/special-purpose beacon row, there is not enough visual evidence to approve the symbol. Required change: BCNCON81 needs an exact local reference crop/render first; then redraw against that crop rather than approving a generic black post.
- `BCNGEN65` — **PASS** (0.92): PASS, not final-approved. The repaired Helm candidate restores the BCNGEN green/white full-chart body with four ordered bands green/white/green/white and black outline/stem. It is scaled larger than OpenCPN but semantically matches the S-52 instruction. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNGEN70` — **PASS** (0.93): PASS, not final-approved. The repaired candidate shows black/yellow/black in the correct vertical order with visible black top and bottom fields. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNGEN71` — **PASS** (0.93): PASS, not final-approved. The repaired candidate shows yellow/black/yellow in the correct order and no longer collapses the pattern. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNGEN76` — **PASS** (0.93): PASS, not final-approved. The repaired candidate shows black/red/black in the correct order with the center red cue retained. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNISD21` — **PASS** (0.86): PASS, not final-approved. The repaired candidate uses the isolated-danger two-ball vertical cue, matching the S-101/OpenCPN/AquaMap symbol family rather than a striped generic beacon. The fill/color treatment can be tuned later, but the hazardous topmark family is now recognizable. Required change: No semantic repair required; final QA should decide exact red/black fill treatment.
- `BCNLAT15` — **PASS** (0.94): PASS, not final-approved. The repaired candidate is a red major lateral beacon body; the previous wrong green band is gone and it matches the S-101/OpenCPN lateral family. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLAT16` — **PASS** (0.94): PASS, not final-approved. The repaired candidate is a green major lateral beacon body; the previous wrong red band is gone and it matches the S-101/OpenCPN lateral family. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLAT21` — **PASS** (0.92): PASS, not final-approved. The repaired candidate is the compact red minor lateral beacon/stake body with no opposite-color band. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLAT22` — **PASS** (0.92): PASS, not final-approved. The repaired candidate is the compact green minor lateral beacon/stake body with no opposite-color band. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLAT23` — **PASS** (0.90): PASS, not final-approved. The repaired candidate uses the red-over-green bifurcation/lateral cue indicated by the OpenCPN row and removes unrelated colors. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLAT50` — **PASS** (0.82): PASS, not final-approved. The repaired candidate is a plain black river stake/pole-like beacon; it is stylized heavier than the tiny OpenCPN mark but the blue fill and wrong body family are gone. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNLTC01` — **PASS** (0.88): PASS, not final-approved. The repaired candidate is a black lattice/tower structure with cross-bracing, matching the S-101/OpenCPN lattice beacon family. It lacks the small base circle seen in S-101 but is semantically recognizable. Required change: No semantic repair required; optional final polish may add/normalize the base detail.
- `BCNSAW13` — **PASS** (0.86): PASS, not final-approved. The repaired candidate restores the safe-water red/white/red cue and no longer presents as a solid black generic beacon. It is close to the AquaMap/OpenCPN safe-water family. Required change: No semantic repair required; final QA may tune body shape/topmark detail.
- `BCNSAW21` — **PASS** (0.86): PASS, not final-approved. The repaired candidate restores the compact minor safe-water red/white/red cue and no longer presents as a solid black generic beacon. Required change: No semantic repair required; final QA may tune body shape/topmark detail.
- `BCNSPP13` — **PASS** (0.82): PASS, not final-approved. The repaired candidate is a yellow special-purpose major beacon body matching the S-101/OpenCPN simplified beacon cue. AquaMap shows an X topmark on a related buoy reference, but the S-101/OpenCPN row evidence supports the plain yellow simplified beacon body. Required change: No semantic repair required; final QA may decide whether this row needs a separate topmark variant.
- `BCNSPP21` — **PASS** (0.82): PASS, not final-approved. The repaired candidate is a compact yellow special-purpose minor beacon body matching the S-101/OpenCPN simplified beacon cue. AquaMap shows an X topmark on a related buoy reference, but the S-101/OpenCPN row evidence supports the plain yellow minor beacon/stake body. Required change: No semantic repair required; final QA may decide whether this row needs a separate topmark variant.
- `BCNSTK02` — **PASS** (0.80): PASS, not final-approved. The repaired candidate is a black stake/pole beacon with the wrong blue band removed. It is heavier than the S-101 pole mark but remains semantically recognizable as a black stake/pole. Required change: No semantic repair required; final QA may slim this to better match the pole silhouette.
- `BCNSTK03` — **PASS** (0.82): PASS, not final-approved. The repaired candidate is a plain black river stake/pole beacon with no unrelated blue band. Required change: No semantic repair required; final QA may slim this to better match the pole silhouette.
- `BCNSTK77` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows ordered green/white/green/white stake bands consistent with the row expectation and OpenCPN cue. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNSTK79` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows ordered red/green/red stake bands consistent with the OpenCPN/S-52 row. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNSTK80` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows ordered green/red/green stake bands consistent with the OpenCPN/S-52 row. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW01` — **PASS** (0.89): PASS, not final-approved. The repaired candidate is a black beacon tower/lattice cue with cross-bracing, replacing the previous generic wedge body and matching S-101/OpenCPN tower family. Required change: No semantic repair required; optional final polish may add/normalize the base detail.
- `BCNTOW63` — **PASS** (0.89): PASS, not final-approved. The repaired candidate shows the red/white tower striping in repeated ordered bands; the row no longer collapses to a two-band or wrong-color mark. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW66` — **PASS** (0.89): PASS, not final-approved. The repaired candidate shows the green/white tower striping in repeated ordered bands visible in the OpenCPN reference. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW70` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows black/yellow/black tower banding in the correct order. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW71` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows yellow/black/yellow tower banding in the correct order. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW74` — **PASS** (0.90): PASS, not final-approved. The repaired candidate shows red/green/red tower banding matching the visible OpenCPN row cue and the previous judge expectation. Required change: No semantic repair required; keep for final style/scale QA only.
- `BCNTOW76` — **PASS** (0.91): PASS, not final-approved. The repaired candidate shows black/red/black tower banding in the correct order. Required change: No semantic repair required; keep for final style/scale QA only.
