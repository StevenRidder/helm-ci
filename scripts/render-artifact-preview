#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MAIN="$ROOT/engine/vulkan/render_artifact_preview.cpp"
HDR="$ROOT/engine/vulkan/render_artifact.h"
BIN="${HELM_RENDER_ARTIFACT_PREVIEW_BIN:-${TMPDIR:-/tmp}/helm-render-artifact-preview}"
CXX_BIN="${CXX:-c++}"

if [ ! -x "$BIN" ] || [ "$MAIN" -nt "$BIN" ] || [ "$HDR" -nt "$BIN" ]; then
  mkdir -p "$(dirname "$BIN")"
  "$CXX_BIN" -std=c++17 -O2 -Wall -Wextra -pedantic -I "$ROOT/engine/vulkan" "$MAIN" -lz -o "$BIN"
fi

exec "$BIN" "$@"
